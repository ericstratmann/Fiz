# This file provides a dummy version of styles.yaml for use during unit
# tests.

bulletin:
    uncaughtAjax: "uncaughtAjax{{: @message}}"
    uncaughtPost: "uncaughtPost{{: @message}}"
    userError:    "userError{{: @message}}"
    error:        "error{{: @message}}"
uncaught:
    html: "uncaught{{: @message}}"
    clearHtml: true