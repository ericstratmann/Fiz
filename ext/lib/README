Add any Java libraries that your extension uses to this directory. If you would
like these to be packaged as part of the extension, add the names of the
libraries on separate lines in the libs.list file.